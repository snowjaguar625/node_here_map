extends ../../layout

block content
    title= title
    #content-padder 
    #content-real
        .ctrl-panel
            span#toggle-ctrl-panel.glyphicon.glyphicon-menu-left
            | &#x9;
            p
                | Click on map to start, Double click to stop adding coordinates.
                | Click &quot;clear &amp; start&quot; button to clear existing polygon and start painting a new one.
                | At least 3 coordinates are required to show a polygon.
            | &#x9;
            input#clear.btn.btn-default.btn-sm(type='button', value='clear & start')
            br
            | &#x9;
            div HLP Service releases which are used in this demo:
            | &#x9;
            #releaseInfoTxt(style='left:0; top:0; z-index: 0;', value='Used Service Releases:/n')
        #mapContainer
    script(src='/javascripts/examples/v31/release-requests.js')
    script(src='/javascripts/examples/v31/polygon_shaper.js')